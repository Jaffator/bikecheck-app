// One of somebody's bikes at /users/:handle/:bikeId. The route exists so a card has
// somewhere to go; the page itself lands with the bike read (slice 4).
import type { ReactElement } from "react";

export function UserProfileBike(): ReactElement | null {
  return null;
}
